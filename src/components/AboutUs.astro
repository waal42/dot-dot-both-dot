---
// src/components/AboutUs.astro
import { Image } from "astro:assets";
import { weddingContent } from "../data/weddingContent";

const { aboutUs } = weddingContent;

const getSizeClasses = (size: string) => {
  switch (size) {
    case "large":
      return "col-span-2 row-span-2";
    case "vertical":
      return "col-span-1 row-span-2";
    case "horizontal":
      return "col-span-2 row-span-1";
    default:
      return "col-span-1 row-span-1";
  }
};
---

<section id="o-nas" class="mx-auto max-w-6xl scroll-mt-24 px-6 py-24">
  <div class="text-center">
    <h2
      class="text-wedding-dark mb-4 flex items-center justify-center gap-4 text-center text-4xl font-bold md:text-5xl animate-fade-in-up"
    >
      <span
        class="morse-decoder text-wedding-medium text-2xl tracking-tighter opacity-30 md:text-3xl hover:opacity-100"
        data-decoded="O"
        >−−−</span
      >
      {aboutUs.title}
      <span
        class="morse-decoder text-wedding-medium text-2xl tracking-tighter opacity-30 md:text-3xl hover:opacity-100"
        data-decoded="S"
        >···</span
      >
    </h2>
    <p class="text-wedding-medium mx-auto max-w-xl text-center text-lg italic opacity-85 mb-16 animate-fade-in-up">
      {aboutUs.motto}
    </p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-24 items-stretch">
    {
      aboutUs.sections.map((section, idx) => (
        <div 
          class="morse-border text-wedding-dark bg-wedding-light shadow-sm hover-card-premium animate-fade-in-up flex flex-col justify-between"
          style={`animation-delay: ${idx * 150}ms`}
        >
          <div class="px-6 py-12 text-center md:px-10 lg:px-12 flex flex-col justify-center h-full">
            <div class="space-y-6">
              <p class="text-wedding-dark text-2xl font-bold md:text-3xl leading-tight">
                {section.title}
              </p>
              {section.content.map((pText) => (
                <p 
                  class="text-wedding-dark mx-auto text-base leading-relaxed font-medium italic opacity-90 text-justify md:text-center" 
                  set:html={pText} 
                />
              ))}
            </div>
          </div>
        </div>
      ))
    }
  </div>

  <div
    id="gallery"
    class="grid auto-rows-[150px] grid-cols-2 gap-3 md:auto-rows-[300px] md:grid-cols-4 md:gap-4 grid-flow-row-dense animate-fade-in-up"
    style="animation-delay: 300ms;"
  >
    {
      aboutUs.gallery.map((item) => (
        <div
          class={`${getSizeClasses(item.size)} group bg-wedding-dark/5 overflow-hidden border border-transparent hover:border-wedding-dark/10 transition-all duration-500 shadow-sm`}
        >
          {item.type === "text" ? (
            <div class="border-wedding-medium/20 bg-wedding-light flex h-full flex-col items-center justify-center border p-4 text-center md:p-8">
              <p class="text-wedding-dark mb-2 text-xl font-bold md:text-2xl leading-tight">
                {item.title}
              </p>
              <div class="morse-line text-wedding-medium/40 my-4 h-[2px] w-24 md:my-6 md:w-40" />
              <p class="text-wedding-medium text-[10px] font-bold tracking-[0.3em] uppercase md:text-xs">
                {item.detail}
              </p>
            </div>
          ) : (
            <a
              href={item.src.src}
              data-pswp-width={item.src.width}
              data-pswp-height={item.src.height}
              target="_blank"
              class="block h-full w-full cursor-zoom-in"
            >
              <Image
                src={item.src}
                alt={item.alt}
                width={800}
                class="h-full w-full scale-105 object-cover grayscale transition-all duration-700 group-hover:scale-100 group-hover:grayscale-0 md:grayscale"
              />
            </a>
          )}
        </div>
      ))
    }
  </div>
</section>

<script>
  import PhotoSwipeLightbox from "photoswipe/lightbox";
  import "photoswipe/style.css";

  const lightbox = new PhotoSwipeLightbox({
    gallery: "#gallery",
    children: "a",
    pswpModule: () => import("photoswipe"),
    padding: { top: 20, bottom: 20, left: 10, right: 10 },
  });
  lightbox.init();
</script>
